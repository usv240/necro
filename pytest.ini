[pytest]
markers =
    live: marks tests as requiring live GitLab API access and a running backend (deselect with -k "not live")
    unit: marks pure-Python tests that run in CI without a live backend
asyncio_mode = auto
