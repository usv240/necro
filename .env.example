# ── Required ──────────────────────────────────────────────────────────────────
GITLAB_TOKEN=glpat-xxxx...         # GitLab personal access token — api + read_api scopes
GITLAB_URL=https://gitlab.com

# Recommended — lifts GitHub API rate limit 60/hr -> 5,000/hr so constraint
# grounding never flakes mid-demo. Read-only public data; no scopes needed.
GITHUB_TOKEN=ghp_xxxx...           # github.com/settings/tokens (classic) — leave all scopes UNCHECKED

GOOGLE_PROJECT_ID=your-gcp-project-id
GOOGLE_LOCATION=us-central1
GEMINI_API_KEY=AIza...             # Google AI Studio → Get API key

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=necro_db

# ── Slack (optional — enables autonomous notifications) ───────────────────────
# Option A: Incoming Webhook (simplest — just paste the webhook URL)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
# Option B: Bot token + channel (full Slack SDK)
SLACK_BOT_TOKEN=xoxb-...
SLACK_CHANNEL_ID=C0...

# ── App ───────────────────────────────────────────────────────────────────────
APP_PORT=8080
APP_URL=https://necro-REPLACE.run.app
OUTPUT_DIR=outputs
DEMO_MODE=false

# ── Autonomous monitoring ─────────────────────────────────────────────────────
MONITOR_INTERVAL_HOURS=24

# ── Optional Vertex AI fallback ───────────────────────────────────────────────
# GEMINI_MODEL=gemini-2.5-flash    # fallback only — Gemini 3 is always tried first
