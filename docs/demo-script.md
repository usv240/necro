# NECRO Demo Script — 3 Minutes

**Prep:** Fullscreen, light mode (`◑`), zoom 90%, terminal footer collapsed.
**URL:** `https://necro-agent-38381883054.us-central1.run.app`

---

### [0:00] Hook — Landing page hero

> "Every team has a graveyard. Features killed for reasons that stopped being true. Dead code nobody knows is safe to remove. NECRO handles both."

---

### [0:12] Scale — Scroll to ROI calculator, drag slider to 50

> "Fifty engineers. Three hundred and fifty thousand dollars a year. That's the problem. Here's what solving it looks like."

**→ Click Launch App**

---

### [0:25] Live Scan — URL field has `ujwal240/necro-demo-graveyard`, click Run Forensic Scan

> "Real GitLab repo. Those are live MCP calls — `list_commits`, `get_commit_diff`, `search_blobs`. Not simulated."

*(let terminal scroll ~10s, then point at a `[SEARCH]` line)*

> "Now it's calling Google Search — verifying whether the kill reason still applies today. Every verdict has a live URL behind it."

---

### [1:05] Results — 1 Revive Now, 2 Candidates, 2 Buried

> "Streaming SSR — blocked by React 17, React is now 19.2.7. Constraint gone, feasibility 90%. Post-quantum TLS — OpenSSL 4.0 shipped in April, also revivable. Adobe Flash — permanently deprecated. NECRO is honest when something should stay dead."

**→ Click Streaming SSR card, point at the evidence section**

> "Live URL, release date, the exact fix. Not an inference — verified."

---

### [1:28] Necrosis — Click Necrosis Registry → gitlab-runner instant demo

> "Opposite question: what's safe to delete. 42.6 years of cumulative dead code age across eleven findings."

**→ Click logDeprecationWarning**

> "Deprecated 2,221 days ago. Zero callers. Risk score one out of ten. That button opens a real GitLab Draft MR with the removal plan already written."

---

### [1:48] Mission Control — Click Mission Control tab, explain before launching

> "Mission Control runs the full loop autonomously — scan, plan, adversarial challenge, then real GitLab artifacts. Watch what happens."

**→ Check Dry run, click Launch Autonomous Mission**

*(let it run, point at phase banners)*

> "The challenger just rejected the revival — no verified evidence the API constraint was fixed. So it downgrades to a discussion issue instead of a Draft MR. It's honest about what it doesn't know."

---

### [2:20] Quick tour — Timeline → Watchlist → Logs (5s each)

> "All scans over time. Repos rescanned every 24h with Slack alerts. Full audit trail of every MR and issue NECRO created."

---

### [2:35] Close — Click logo, land on hero

> "476 scans. 524 findings. Zero load-bearing code ever flagged for deletion. Both problems, under two minutes, any public GitLab repo."

---

## Three lines that must land

1. *"Live MCP calls — not simulated, happening as we speak."*
2. *"A second model on separate Vertex AI infrastructure arguing against every revival it sees."*
3. *"476 scans. 524 findings. Zero load-bearing code ever flagged for deletion."*

---

## Backup

If live scan is slow → click **necro-demo** instant demo chip, say "pre-analysed from cache, same output."
